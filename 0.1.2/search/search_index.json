{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"OMGrab SDK","text":"<p>SDK for egocentric data collection with OMGrab hardware.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install omgrab\n</code></pre>"},{"location":"#overview","title":"Overview","text":"<p>The omgrab SDK provides the core runtime for egocentric data collection on the OMGrab streaming device. The SDK is used to build the core runtime application, which supports:</p> <ul> <li>User activated recording start/stop on physical button press</li> <li>Minute-chunked clip recording of various stream types to disk for crash safety</li> <li>Automatic recording chunk merging into a standardized MKV container on stop</li> <li>OLED status display with camera preview mode</li> <li>WiFi hotspot for authentication</li> <li>Automatic network and device health monitoring</li> <li>Network file share of completed recordings using Samba</li> </ul>"},{"location":"#key-components","title":"Key components","text":"<ul> <li>devices -- hardware capture device abstraction (<code>CaptureDevice</code> protocol)</li> <li>cameras -- frame sources for RGB, depth, and USB cameras</li> <li>sensors -- non-video data sources (e.g., IMU)</li> <li>recording -- chunked MKV writing with multi-stream support</li> <li>runtime -- recording lifecycle, network monitoring, device status</li> <li>workflows -- state machine coordinating the full device lifecycle</li> <li>gpio -- LED, buzzer, and button control</li> <li>display -- OLED status dashboard and live camera preview</li> </ul>"},{"location":"#quick-start","title":"Quick start","text":"<p>This example initializes an OAK-D camera, captures 10 seconds of synchronized RGB + depth video, and writes the result to a single MKV file on disk.</p> <pre><code>import pathlib\n\nfrom omgrab.cameras import CameraConfig\nfrom omgrab.devices import OakDCaptureDevice\nfrom omgrab.recording import ChunkedWriter, VideoStreamConfig\n\n# 1. Configure cameras and streams\nrgb_config = CameraConfig(fps=25, width=1280, height=800)\ndepth_config = CameraConfig(fps=25, width=640, height=400)\n\noutput_dir = pathlib.Path('./my_recording')\noutput_dir.mkdir(exist_ok=True)\n\nvideo_streams = {\n    'rgb': VideoStreamConfig(\n        width=rgb_config.width,\n        height=rgb_config.height,\n        fps=rgb_config.fps,\n        codec='libx264',\n        bitrate=4_000_000,\n        input_pixel_format='rgb24',\n        output_pixel_format='yuv420p',\n        metadata={'type': 'rgb'},\n    ),\n    'depth': VideoStreamConfig(\n        width=depth_config.width,\n        height=depth_config.height,\n        fps=depth_config.fps,\n        codec='ffv1',\n        input_pixel_format='gray16le',\n        output_pixel_format='gray16le',\n        metadata={'type': 'depth'},\n    ),\n}\n\n# 2. Initialize the capture device\ndevice = OakDCaptureDevice(rgb_config, depth_config)\n\n# 3. Initialize the writer and get encoder queues\nwriter = ChunkedWriter(\n    name='my_recording',\n    output_directory=output_dir,\n    stream_configs=video_streams,\n    chunk_length_s=60.0,\n)\nrgb_queue = writer.get_encoder_queue('rgb')\ndepth_queue = writer.get_encoder_queue('depth')\n\n# 4. Record frames from the capture device to disk\nwith device:\n    device.wait_until_ready(timeout=10.0)\n    rgb_camera = device.get_rgb_camera()\n    depth_camera = device.get_depth_camera()\n\n    with writer:\n        for _ in range(25 * 10):  # 10 seconds at 25 fps\n            rgb_frame, rgb_ts = rgb_camera.get_next_frame(timeout_s=1.0)\n            depth_frame, depth_ts = depth_camera.get_next_frame(timeout_s=1.0)\n\n            rgb_queue.put((rgb_frame, rgb_ts), block=False)\n            depth_queue.put((depth_frame, depth_ts), block=False)\n</code></pre>"},{"location":"#device-runtime","title":"Device runtime","text":"<p>The OMGrab device runtime application built using the OMGrab SDK is included under Device Runtime.</p>"},{"location":"runtime-app/","title":"Device Runtime","text":"<p>The <code>app/</code> directory contains the runtime application that wires the SDK components together into the full omgrab device runtime. It is deployed as a Docker container and is built using the OMGrab SDK.</p>"},{"location":"runtime-app/#architecture","title":"Architecture","text":"<p>The application entry point (<code>app/__main__.py</code>) creates and connects:</p> <ol> <li>Capture device -- an <code>OakDCaptureDevice</code> producing synchronized RGB + depth frames and IMU data</li> <li>Recording manager -- manages recording sessions, chunk naming, and merge-on-stop</li> <li>State machine -- coordinates BOOT \u2192 IDLE \u2194 RECORDING transitions, plus WIFI_SETUP and SHUTDOWN</li> <li>GPIO controller -- drives LEDs and buzzer based on state machine transitions</li> <li>Button monitor -- polls the physical button and dispatches short-press (record toggle) and long-press (WiFi setup) events</li> <li>Network monitor -- probes connectivity and broadcasts OFFLINE / NETWORK_ONLY / ONLINE state changes</li> <li>Device status manager -- aggregates system metrics (CPU, memory, disk, temperature) and operational state</li> <li>Screen manager -- renders status and live camera preview to the OLED display</li> </ol> <p>On startup, the app also runs <code>merge_orphaned_chunks()</code> to recover any recordings left incomplete by a prior crash.</p>"},{"location":"runtime-app/#configuration","title":"Configuration","text":"<p>All configuration is via environment variables, set in <code>docker-compose.yml</code>:</p> Variable Default Description <code>DEVICE_ID</code> <code>unknown-device</code> Unique identifier for this device <code>AGENT_VERSION</code> <code>v0.0.1</code> Software version (set at build time) <code>WRIST_CAMERAS_ENABLED</code> <code>false</code> Enable auxiliary USB wrist cameras <code>WIFI_CONNECT_IMAGE</code> <code>wifi-connect:latest</code> Docker image for the WiFi setup container <code>DEPTHAI_LEVEL</code> <code>warn</code> DepthAI SDK log level"},{"location":"runtime-app/#paths","title":"Paths","text":"Path (in container) Host mount Purpose <code>/data/spool/capture/</code> <code>/opt/omgrab/data/spool/capture/</code> In-progress recording chunks <code>/data/spool/output/</code> <code>/opt/omgrab/data/spool/output/</code> Merged MKV output files <code>/var/run/docker.sock</code> <code>/var/run/docker.sock</code> Docker socket for WiFi container management <code>/button-trigger</code> <code>/opt/omgrab/button-trigger</code> Remote button simulation (touch a file to trigger)"},{"location":"runtime-app/#stream-configuration","title":"Stream configuration","text":"<p>The app configures the following streams per recording:</p> Stream Codec Resolution FPS Notes <code>egocentric_rgb</code> H.264 (libx264) 1280x800 25 Baseline profile, superfast preset, 4 Mbps <code>egocentric_depth</code> FFV1 (lossless) 640x400 25 16-bit grayscale <code>imu</code> ASS subtitle -- 100 Hz JSON-encoded IMU samples <code>left_wrist</code> H.264 1280x720 15 Only when <code>WRIST_CAMERAS_ENABLED=true</code> <code>right_wrist</code> H.264 1280x720 15 Only when <code>WRIST_CAMERAS_ENABLED=true</code>"},{"location":"runtime-app/#installation","title":"Installation","text":"<p>The installer (<code>setup/install.sh</code>) provisions a Raspberry Pi from scratch. It must be run as root on an ARM64 device:</p> <pre><code>sudo ./setup/install.sh --device-id my-device-001\n</code></pre> <p>The installer performs the following steps:</p> <ol> <li>Installs Docker and Docker Compose if not present</li> <li>Creates <code>/opt/omgrab/</code> with data directories and a <code>.env</code> file containing the device ID</li> <li>Configures persistent journald logging (200 MB cap)</li> <li>Enables I2C (required for the battery monitor on <code>/dev/i2c-1</code>)</li> <li>Installs the early-led boot indicator service (see Services below)</li> <li>Installs WiFi stability fixes (NetworkManager dispatcher script)</li> <li>Sets up a Samba file share at <code>smb://&lt;hostname&gt;.local/omgrab</code> exposing the recordings output directory (user: <code>omgrab</code>, password: <code>omgrab</code>)</li> <li>Builds the Docker images locally from the current source tree</li> <li>Installs and enables the <code>omgrab.service</code> systemd unit</li> <li>Optionally starts the service immediately</li> </ol>"},{"location":"runtime-app/#uninstallation","title":"Uninstallation","text":"<p>The runtime can be uninstalled using:</p> <pre><code>sudo ./setup/uninstall.sh\n</code></pre> <p>This stops the service, removes systemd units, tears down Docker containers and images, and removes the Samba share. Optionally deletes <code>/opt/omgrab/</code> (including all recorded data) if confirmed interactively.</p>"},{"location":"runtime-app/#services","title":"Services","text":"<p>Several systemd services are used to power the runtime:</p>"},{"location":"runtime-app/#omgrabservice","title":"omgrab.service","text":"<p>The primary systemd unit. Runs the Docker Compose stack and restarts automatically on failure (up to 5 times per 60 seconds).</p> <ul> <li>Starts after: <code>docker.service</code>, <code>systemd-udev-settle.service</code>, <code>NetworkManager.service</code></li> <li>Working directory: <code>/opt/omgrab</code></li> <li>Start command: <code>docker compose up --abort-on-container-exit --remove-orphans</code></li> <li>Stop command: <code>docker compose down</code></li> <li>Restart policy: always, 5 second delay</li> </ul> <p>Before starting, the service stops the early-led indicator and loads the <code>uvcvideo</code> kernel module (for USB cameras).</p> <p>Useful commands:</p> <pre><code>sudo systemctl start omgrab        # Start the runtime\nsudo systemctl stop omgrab         # Stop the runtime\nsudo systemctl status omgrab       # Check status\nsudo journalctl -u omgrab -f       # Follow logs\nsudo journalctl -u omgrab --boot=-1  # Logs from previous boot\n</code></pre>"},{"location":"runtime-app/#early-ledservice","title":"early-led.service","text":"<p>A minimal boot indicator that blinks the green LED (GPIO 17) at ~5 Hz immediately after the root filesystem is mounted. This gives the user visual feedback that the device is booting before Docker and the main application are ready. The omgrab service stops it on startup and takes over LED control via the GPIO controller.</p> <ul> <li>Starts at: <code>sysinit.target</code> (very early boot, before most services)</li> <li>Stopped by: <code>omgrab.service</code> via <code>ExecStartPre</code></li> </ul>"},{"location":"runtime-app/#samba-share","title":"Samba share","text":"<p>Not a custom service -- uses the system <code>smbd</code>. The installer adds an <code>[omgrab]</code> share definition pointing to <code>/opt/omgrab/data/spool/output/</code> so recordings can be accessed over the local network at <code>smb://&lt;hostname&gt;.local/omgrab</code>.</p>"},{"location":"runtime-app/#deployment","title":"Deployment","text":"<p>The compose stack defines two containers:</p> <ul> <li>omgrab-runtime -- the main application, running privileged with host networking, GPIO chip access (<code>/dev/gpiochip0</code>), USB device access, and the Docker socket mounted for managing the WiFi container</li> <li>wifi-connect -- a separate Rust-based container (built from <code>wifi-connect/</code>) that creates a captive portal hotspot for WiFi provisioning. Not started by default -- launched on-demand by the runtime via Docker socket when the user long-presses the button. This container is based on the wifi-connect project by BalenaOS.</li> </ul>"},{"location":"runtime-app/#shutdown","title":"Shutdown","text":"<p>On <code>SIGINT</code> or <code>SIGTERM</code>, the app performs a graceful shutdown sequence:</p> <ol> <li>State machine transitions to SHUTDOWN (LEDs indicate shutdown)</li> <li>Active recording is stopped and chunks are merged</li> <li>Network monitor, screen manager, and device status manager are stopped</li> <li>Process exits</li> </ol>"},{"location":"api/cameras/","title":"cameras","text":"<p>Camera classes represent a particular capture session or recording duration. A camera is typically created from a capture device, though cameras can also operate independently (e.g. opening a USB device directly via OpenCV). Cameras are started and stopped each time the user begins and ends a recording, while the underlying capture device may remain powered on across multiple recordings.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras","title":"<code>omgrab.cameras.cameras</code>","text":"<p>Camera module for omgrab.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras.CameraConfig","title":"<code>CameraConfig(fps, width, height)</code>  <code>dataclass</code>","text":"<p>Base camera configuration.</p> <p>Attributes:</p> Name Type Description <code>fps</code> <code>int</code> <p>Target capture frame rate.</p> <code>width</code> <code>int</code> <p>Frame width in pixels.</p> <code>height</code> <code>int</code> <p>Frame height in pixels.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras.FrameUnavailableError","title":"<code>FrameUnavailableError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Exception raised when a frame is not available.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras.Camera","title":"<code>Camera(config, enforce_frame_timing=True)</code>","text":"<p>               Bases: <code>Generic[FrameT]</code>, <code>ABC</code></p> <p>Camera class for omgrab.</p> <p>Initialize the camera.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>CameraConfig</code> <p>Camera configuration.</p> required <code>enforce_frame_timing</code> <code>bool</code> <p>If True, get_next_frame will sleep to enforce the configured FPS. Set to False for queue-based cameras where the hardware already controls frame timing.</p> <code>True</code>"},{"location":"api/cameras/#omgrab.cameras.cameras.Camera.config","title":"<code>config</code>  <code>property</code>","text":"<p>Get the camera configuration.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras.Camera.get_next_frame","title":"<code>get_next_frame(timeout_s=None)</code>  <code>abstractmethod</code>","text":"<p>Get the next frame from the camera.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras.Camera.setup","title":"<code>setup()</code>  <code>abstractmethod</code>","text":"<p>Setup the camera.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras.Camera.close","title":"<code>close()</code>  <code>abstractmethod</code>","text":"<p>Close the camera.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras.Camera.__enter__","title":"<code>__enter__()</code>","text":"<p>Enter the context manager.</p>"},{"location":"api/cameras/#omgrab.cameras.cameras.Camera.__exit__","title":"<code>__exit__(exc_type, exc_value, traceback)</code>","text":"<p>Exit the context manager.</p>"},{"location":"api/cameras/#omgrab.cameras.oakd_camera","title":"<code>omgrab.cameras.oakd_camera</code>","text":"<p>OAK-D camera implementations.</p> <p>Camera classes that read frames from queues populated by the OAK-D capture device (see omgrab.devices.oakd_capture_device). Each camera instance represents a single stream (RGB or depth) for the duration of a recording session.</p>"},{"location":"api/cameras/#omgrab.cameras.oakd_camera.OakDRGBCamera","title":"<code>OakDRGBCamera(config, frame_queue)</code>","text":"<p>               Bases: <code>QueueReaderCamera[RGBFrame]</code></p> <p>OakD RGB camera class.</p> <p>Initialize the OakD RGB camera.</p>"},{"location":"api/cameras/#omgrab.cameras.oakd_camera.OakDDepthCamera","title":"<code>OakDDepthCamera(config, frame_queue)</code>","text":"<p>               Bases: <code>QueueReaderCamera[DepthFrame]</code></p> <p>OakD Depth camera class.</p> <p>Initialize the OakD Depth camera.</p>"},{"location":"api/cameras/#omgrab.cameras.queue_reader_camera","title":"<code>omgrab.cameras.queue_reader_camera</code>","text":"<p>Camera implementation that reads frames from a queue.</p>"},{"location":"api/cameras/#omgrab.cameras.queue_reader_camera.QueueReaderCamera","title":"<code>QueueReaderCamera(config, frame_queue)</code>","text":"<p>               Bases: <code>Camera[FrameT]</code>, <code>Generic[FrameT]</code></p> <p>Camera that reads frames from a queue.</p> <p>Initialize the queue reader camera.</p>"},{"location":"api/cameras/#omgrab.cameras.queue_reader_camera.QueueReaderCamera.setup","title":"<code>setup()</code>","text":"<p>Setup the queue reader camera.</p>"},{"location":"api/cameras/#omgrab.cameras.queue_reader_camera.QueueReaderCamera.close","title":"<code>close()</code>","text":"<p>Close the queue reader camera.</p>"},{"location":"api/cameras/#omgrab.cameras.queue_reader_camera.QueueReaderCamera.get_next_frame","title":"<code>get_next_frame(timeout_s=None)</code>","text":"<p>Get the next frame from the queue.</p>"},{"location":"api/cameras/#omgrab.cameras.queue_reader_camera.QueueReaderCamera.flush_queue","title":"<code>flush_queue()</code>","text":"<p>Flush all pending frames from the queue.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of frames flushed.</p>"},{"location":"api/cameras/#omgrab.cameras.usb_camera","title":"<code>omgrab.cameras.usb_camera</code>","text":"<p>USB camera implementation using OpenCV VideoCapture.</p>"},{"location":"api/cameras/#omgrab.cameras.usb_camera.USBCamera","title":"<code>USBCamera(config, device_path='', usb_port_path='')</code>","text":"<p>               Bases: <code>Camera[RGBFrame]</code></p> <p>Camera that reads frames from a USB camera via OpenCV.</p> <p>Initialize the USB camera.</p> <p>Exactly one of device_path or usb_port_path must be provided.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>CameraConfig</code> <p>Camera configuration (fps, width, height).</p> required <code>device_path</code> <code>str</code> <p>Path to the video device (e.g. '/dev/video2').</p> <code>''</code> <code>usb_port_path</code> <code>str</code> <p>USB port path (e.g. '3-2'). The device path is resolved from sysfs each time setup() is called.</p> <code>''</code>"},{"location":"api/cameras/#omgrab.cameras.usb_camera.USBCamera.setup","title":"<code>setup()</code>","text":"<p>Open the USB camera and configure resolution/fps.</p>"},{"location":"api/cameras/#omgrab.cameras.usb_camera.USBCamera.close","title":"<code>close()</code>","text":"<p>Release the USB camera.</p>"},{"location":"api/cameras/#omgrab.cameras.usb_camera.USBCamera.get_next_frame","title":"<code>get_next_frame(timeout_s=None)</code>","text":"<p>Read the next frame from the USB camera.</p> <p>Parameters:</p> Name Type Description Default <code>timeout_s</code> <code>Optional[float]</code> <p>Not used. On V4L2 with BUFFERSIZE=1, grab() blocks for at most one frame period (~33ms at 30fps). If the device is disconnected, grab() returns False immediately.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[RGBFrame, datetime]</code> <p>Tuple of (frame, timestamp).</p> <p>Raises:</p> Type Description <code>FrameUnavailableError</code> <p>If the frame could not be read.</p>"},{"location":"api/devices/","title":"devices","text":"<p>Capture devices represent the physical hardware (or driver/pipeline) that produces frame data. A device owns the underlying resources (USB handles, DepthAI pipelines, etc.) and can be powered on or off independently of any recording. Cameras, by contrast, are lightweight per-recording wrappers typically created from a capture device.</p> <p>For example, the OAK-D capture device manages the DepthAI pipeline and hardware connection, while <code>OakDRGBCamera</code> and <code>OakDDepthCamera</code> are session-scoped readers created for each recording. The device may stay powered between recordings (e.g. for preview), but cameras are created fresh each time.</p>"},{"location":"api/devices/#omgrab.devices.capture_device","title":"<code>omgrab.devices.capture_device</code>","text":"<p>Capture device abstraction.</p> <p>A capture device represents the physical hardware (or driver/pipeline) that owns the underlying resources required to produce one or more camera streams. Devices can be powered on or off independently of any recording session. Cameras, by contrast, are lightweight per-recording wrappers typically created from a capture device, and are started/stopped each time the user begins and ends a recording.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.PreviewUnavailableError","title":"<code>PreviewUnavailableError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Raised when preview is not available on a capture device.</p> <p>This occurs when a device was not configured with a preview config, or does not support preview at all.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice","title":"<code>CaptureDevice</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>A physical capture device that can be opened/closed and queried for health.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice.label","title":"<code>label</code>  <code>property</code>","text":"<p>Human-readable label for this device (e.g. 'oakd', 'left_wrist').</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice.connected","title":"<code>connected</code>  <code>property</code>","text":"<p>Whether the underlying device appears connected/healthy.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice.ready","title":"<code>ready</code>  <code>property</code>","text":"<p>Whether the underlying device is ready to capture frames.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice.device_type","title":"<code>device_type</code>  <code>property</code>","text":"<p>Return a device type identifier, if available.</p> <p>This is an opaque string whose meaning is defined by the concrete device implementation. For example, OAK-D devices report variants like <code>'oakd_pro_wide'</code>. Returns None if the device has not been started, does not support type detection, or detection failed.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice.wait_until_ready","title":"<code>wait_until_ready(timeout=None)</code>","text":"<p>Wait until the underlying device is ready to capture frames.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice.__enter__","title":"<code>__enter__()</code>","text":"<p>Acquire underlying resources needed for capture.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice.__exit__","title":"<code>__exit__(exc_type, exc_value, traceback)</code>","text":"<p>Release underlying resources needed for capture.</p>"},{"location":"api/devices/#omgrab.devices.capture_device.CaptureDevice.preview","title":"<code>preview()</code>","text":"<p>Start a lightweight preview pipeline.</p> <p>Returns a context manager that runs a preview-only pipeline while active. The device must not be in recording mode (i.e. <code>__enter__</code> must not be active). Implementations that do not support preview should raise <code>RuntimeError</code>.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device","title":"<code>omgrab.devices.oakd_capture_device</code>","text":"<p>OAK-D capture device implementation.</p> <p>The capture device represents the physical OAK-D hardware and owns the DepthAI pipeline lifecycle. It can be opened and closed independently of the cameras that read from its frame queues. Cameras are lightweight wrappers that represent a particular recording session; the device may outlive many camera instances across multiple recordings.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.PipelineConfig","title":"<code>PipelineConfig(warmup_frames=10, startup_grace_s=3.0, shutdown_timeout_s=2.0)</code>  <code>dataclass</code>","text":"<p>Configuration for the OAK-D pipeline runner.</p> <p>Attributes:</p> Name Type Description <code>warmup_frames</code> <code>int</code> <p>Number of initial frames to discard before marking the device as ready.</p> <code>startup_grace_s</code> <code>float</code> <p>Grace period after pipeline start during which stale-frame detection is relaxed.</p> <code>shutdown_timeout_s</code> <code>float</code> <p>Timeout for joining the capture thread during device shutdown.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice","title":"<code>OakDCaptureDevice(rgb_config, depth_config, max_queue_size=200, imu_rate_hz=0, preview_config=None, pipeline_config=_DEFAULT_PIPELINE_CONFIG)</code>","text":"<p>               Bases: <code>CaptureDevice</code></p> <p>OakD device used to populate two camera queues from one source.</p> <p>Initialize the OakD device.</p> <p>Parameters:</p> Name Type Description Default <code>rgb_config</code> <code>CameraConfig</code> <p>Configuration for the RGB camera.</p> required <code>depth_config</code> <code>CameraConfig</code> <p>Configuration for the depth camera.</p> required <code>max_queue_size</code> <code>int</code> <p>Maximum queue size for frame/data buffers.</p> <code>200</code> <code>imu_rate_hz</code> <code>int</code> <p>IMU sampling rate in Hz. 0 to disable IMU.</p> <code>0</code> <code>preview_config</code> <code>Optional[CameraConfig]</code> <p>Optional configuration for the preview camera. If provided, enables the preview() context manager and get_preview_camera(). Uses a lightweight RGB-only pipeline.</p> <code>None</code> <code>pipeline_config</code> <code>PipelineConfig</code> <p>Configuration for the pipeline runner.</p> <code>_DEFAULT_PIPELINE_CONFIG</code>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.label","title":"<code>label</code>  <code>property</code>","text":"<p>Human-readable label for this device.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.connected","title":"<code>connected</code>  <code>property</code>","text":"<p>Check if the OakD camera is connected.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.ready","title":"<code>ready</code>  <code>property</code>","text":"<p>Check if the OakD camera is ready.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.rgb_queue","title":"<code>rgb_queue</code>  <code>property</code>","text":"<p>Get the rgb queue.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.depth_queue","title":"<code>depth_queue</code>  <code>property</code>","text":"<p>Get the depth queue.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.imu_queue","title":"<code>imu_queue</code>  <code>property</code>","text":"<p>Get the IMU data queue, or None if IMU is disabled.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.imu_enabled","title":"<code>imu_enabled</code>  <code>property</code>","text":"<p>Whether IMU data collection is enabled.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.device_type","title":"<code>device_type</code>  <code>property</code>","text":"<p>Return the detected OAK-D device type as a string.</p> <p>The device type is read from EEPROM during pipeline startup. Returns None if the device has not been started or if type detection failed.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>One of the OakDDeviceType values (e.g. 'oakd_pro_wide'), or None.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.wait_until_ready","title":"<code>wait_until_ready(timeout=None)</code>","text":"<p>Wait until the OakD camera is ready to capture frames.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.get_rgb_camera","title":"<code>get_rgb_camera()</code>","text":"<p>Get the RGB camera.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.get_depth_camera","title":"<code>get_depth_camera()</code>","text":"<p>Get the depth camera.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.get_imu_source","title":"<code>get_imu_source()</code>","text":"<p>Get a data source for reading IMU data.</p> <p>Returns:</p> Type Description <code>QueueReaderSensor</code> <p>QueueReaderSensor wrapping the IMU queue.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If IMU is not enabled.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.get_preview_camera","title":"<code>get_preview_camera()</code>","text":"<p>Get the preview RGB camera.</p> <p>The returned camera reads from the preview queue and only produces frames while the <code>preview()</code> context manager is active.</p> <p>Returns:</p> Type Description <code>OakDRGBCamera</code> <p>An OakDRGBCamera reading from the preview queue.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If preview_config was not provided at construction.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.preview","title":"<code>preview()</code>","text":"<p>Context manager that runs a lightweight RGB-only preview pipeline.</p> <p>The preview pipeline is mutually exclusive with the recording pipeline (<code>__enter__</code>). Starting preview while recording is active (or vice-versa) raises <code>RuntimeError</code>.</p> <p>Yields:</p> Name Type Description <code>OakDCaptureDevice</code> <code>OakDCaptureDevice</code> <p>This device instance.</p> <p>Raises:</p> Type Description <code>PreviewUnavailableError</code> <p>If preview_config was not provided.</p> <code>RuntimeError</code> <p>If the recording pipeline is already running.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.__enter__","title":"<code>__enter__()</code>","text":"<p>Setup the oakd capture device.</p>"},{"location":"api/devices/#omgrab.devices.oakd_capture_device.OakDCaptureDevice.__exit__","title":"<code>__exit__(exc_type, exc_value, traceback)</code>","text":"<p>Close the oakd capture device.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device","title":"<code>omgrab.devices.usb_capture_device</code>","text":"<p>USB capture device.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice","title":"<code>USBCaptureDevice(config, usb_port_path, label, preview_config=None)</code>","text":"<p>               Bases: <code>CaptureDevice</code></p> <p>Capture device wrapping a single USB camera identified by USB port path.</p> <p>Implements the CaptureDevice protocol. The camera is opened/closed per-recording by the capture thread (via the Camera context manager), not by this device's enter/exit.</p> <p>Initialize the USB capture device.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>CameraConfig</code> <p>Camera configuration (fps, width, height).</p> required <code>usb_port_path</code> <code>str</code> <p>USB port path (e.g. '3-2') used to discover the /dev/videoN device via sysfs.</p> required <code>label</code> <code>str</code> <p>Human-readable label for logging (e.g. 'left_wrist').</p> required <code>preview_config</code> <code>Optional[CameraConfig]</code> <p>Optional camera configuration for preview mode. If not provided, preview() will raise PreviewUnavailableError.</p> <code>None</code>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.label","title":"<code>label</code>  <code>property</code>","text":"<p>Human-readable label for this device.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.connected","title":"<code>connected</code>  <code>property</code>","text":"<p>Whether the USB camera is present and openable.</p> <p>Resolves the USB port path to a /dev/videoN device via sysfs, then verifies the device node can actually be opened. A device may appear in sysfs even with a loose connection; the open check catches that.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.ready","title":"<code>ready</code>  <code>property</code>","text":"<p>USB cameras have no warmup; ready if connected.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.device_type","title":"<code>device_type</code>  <code>property</code>","text":"<p>USB cameras do not have a device type.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.wait_until_ready","title":"<code>wait_until_ready(timeout=None)</code>","text":"<p>USB cameras are immediately ready.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.get_camera","title":"<code>get_camera()</code>","text":"<p>Get the USB camera instance.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.get_preview_camera","title":"<code>get_preview_camera()</code>","text":"<p>Get the preview camera.</p> <p>The returned camera reads from the preview queue and only produces frames while the <code>preview()</code> context manager is active.</p> <p>Returns:</p> Type Description <code>QueueReaderCamera[RGBFrame]</code> <p>A QueueReaderCamera reading from the preview queue.</p> <p>Raises:</p> Type Description <code>PreviewUnavailableError</code> <p>If preview_config was not provided.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.__enter__","title":"<code>__enter__()</code>","text":"<p>No-op; the camera is opened by the capture thread.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.__exit__","title":"<code>__exit__(exc_type, exc_value, traceback)</code>","text":"<p>No-op; the camera is closed by the capture thread.</p>"},{"location":"api/devices/#omgrab.devices.usb_capture_device.USBCaptureDevice.preview","title":"<code>preview()</code>","text":"<p>Context manager that runs a preview capture loop.</p> <p>Opens a separate USBCamera with the preview configuration and reads frames in a background thread, pushing them into a queue. The preview is mutually exclusive with recording.</p> <p>Yields:</p> Name Type Description <code>USBCaptureDevice</code> <code>USBCaptureDevice</code> <p>This device instance.</p> <p>Raises:</p> Type Description <code>PreviewUnavailableError</code> <p>If preview_config was not provided.</p> <code>RuntimeError</code> <p>If preview is already active.</p>"},{"location":"api/display/","title":"display","text":"<p>OLED status dashboard and live camera preview.</p>"},{"location":"api/display/#omgrab.display.screen_manager","title":"<code>omgrab.display.screen_manager</code>","text":"<p>Screen manager for rendering device status and notifications to the OLED.</p> <p>This module owns the ScreenWriter and a background thread that periodically queries DeviceStatusManager to render a status dashboard. It also accepts pushed notifications from other components for time-critical messages.</p> <p>When camera preview is active, the manager takes ownership of the capture device's preview context (via <code>contextlib.ExitStack</code>) and renders live dithered camera frames at the maximum rate the I2C bus allows.</p>"},{"location":"api/display/#omgrab.display.screen_manager.Notification","title":"<code>Notification(message, duration_s=3.0, priority=0, warning=False, created_at=time.monotonic())</code>  <code>dataclass</code>","text":"<p>A transient notification to display on screen.</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>str</code> <p>Text to display.  For warnings, use a newline to separate into two lines.</p> <code>duration_s</code> <code>float</code> <p>How long to show the notification.</p> <code>priority</code> <code>int</code> <p>Higher values are shown first.</p> <code>warning</code> <code>bool</code> <p>If True, render with a triangle alert symbol.</p> <code>created_at</code> <code>float</code> <p>Monotonic timestamp when the notification was created.</p>"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager","title":"<code>ScreenManager(writer=None, refresh_interval_s=_DEFAULT_REFRESH_INTERVAL_S, screen_timeout_s=_DEFAULT_SCREEN_TIMEOUT_S)</code>","text":"<p>Manages the OLED display with periodic status updates and push notifications.</p> <p>The manager runs a background thread that: 1. Checks for pending notifications (highest priority first). 2. If no notification is active, renders the current device status. 3. Pushes the rendered image to the ScreenWriter.</p> <p>Other components can call show_notification() from any thread to display a transient message immediately.</p> <p>Initialize the screen manager.</p> <p>Parameters:</p> Name Type Description Default <code>writer</code> <code>Optional[ScreenWriter]</code> <p>ScreenWriter instance. If None, a default one is created.</p> <code>None</code> <code>refresh_interval_s</code> <code>float</code> <p>How often to refresh the display (seconds).</p> <code>_DEFAULT_REFRESH_INTERVAL_S</code> <code>screen_timeout_s</code> <code>float</code> <p>Seconds of inactivity before blanking the screen.</p> <code>_DEFAULT_SCREEN_TIMEOUT_S</code>"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.display_available","title":"<code>display_available</code>  <code>property</code>","text":"<p>Whether the OLED display hardware is currently available.</p>"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.set_preview_source","title":"<code>set_preview_source(capture_device, preview_camera)</code>","text":"<p>Set the capture device and preview camera for camera preview mode.</p> <p>Parameters:</p> Name Type Description Default <code>capture_device</code> <code>CaptureDevice</code> <p>The capture device that provides the preview context manager.</p> required <code>preview_camera</code> <code>Camera</code> <p>The camera to read preview frames from.</p> required"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.set_device_status_manager","title":"<code>set_device_status_manager(device_status_manager)</code>","text":"<p>Set the device status manager reference.</p> <p>Parameters:</p> Name Type Description Default <code>device_status_manager</code> <code>DeviceStatusManager</code> <p>The DeviceStatusManager to query for status.</p> required"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.start","title":"<code>start()</code>","text":"<p>Start the background display refresh thread.</p>"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.shutdown","title":"<code>shutdown()</code>","text":"<p>Stop the background thread and clean up the display.</p>"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.start_preview","title":"<code>start_preview()</code>","text":"<p>Start live camera preview on the display.</p> <p>Enters the capture device's preview context (starting the lightweight RGB-only pipeline) and switches the render loop to display dithered camera frames at the maximum rate the I2C bus allows.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the preview source has not been set, the device cannot start preview, or the display is unavailable.</p>"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.stop_preview","title":"<code>stop_preview()</code>","text":"<p>Stop live camera preview and return to normal status display.</p>"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.wake","title":"<code>wake()</code>","text":"<p>Reset the screen timeout and wake the display immediately.</p> <p>Call from any thread (e.g. on button press) to turn the screen back on if it has timed out.</p>"},{"location":"api/display/#omgrab.display.screen_manager.ScreenManager.show_notification","title":"<code>show_notification(message, duration_s=3.0, priority=0, warning=False)</code>","text":"<p>Push a transient notification to the display.</p> <p>Can be called from any thread. The notification will be shown immediately (waking the display thread) and auto-dismissed after duration_s seconds.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Text to display.  For warnings, use a newline to separate into two lines.</p> required <code>duration_s</code> <code>float</code> <p>How long to show the notification (seconds).</p> <code>3.0</code> <code>priority</code> <code>int</code> <p>Higher values are shown first when multiple are queued.</p> <code>0</code> <code>warning</code> <code>bool</code> <p>If True, render with a triangle alert symbol.</p> <code>False</code>"},{"location":"api/display/#omgrab.display.screen_writer","title":"<code>omgrab.display.screen_writer</code>","text":"<p>Low-level OLED screen writer for SSD1306 displays over I2C.</p> <p>This module provides a hardware abstraction for writing PIL images to an SSD1306 OLED display. It handles initialization, reconnection on failure, and graceful degradation when the hardware is absent.</p> <p>Modeled after the battery_monitor module's retry/reconnect pattern.</p>"},{"location":"api/display/#omgrab.display.screen_writer.ScreenWriter","title":"<code>ScreenWriter(i2c_bus=1, i2c_address=60, width=128, height=64)</code>","text":"<p>Hardware abstraction for SSD1306 OLED display over I2C.</p> <p>Handles initialization, display writes, and automatic reconnection. This class is not thread-safe on its own; callers should ensure that display() and clear() are called from a single thread.</p> <p>Initialize the screen writer.</p> <p>Parameters:</p> Name Type Description Default <code>i2c_bus</code> <code>int</code> <p>I2C bus number (default: 1).</p> <code>1</code> <code>i2c_address</code> <code>int</code> <p>I2C address of the SSD1306 (default: 0x3C).</p> <code>60</code> <code>width</code> <code>int</code> <p>Display width in pixels (default: 128).</p> <code>128</code> <code>height</code> <code>int</code> <p>Display height in pixels (default: 64).</p> <code>64</code>"},{"location":"api/display/#omgrab.display.screen_writer.ScreenWriter.available","title":"<code>available</code>  <code>property</code>","text":"<p>True if the screen hardware is available.</p>"},{"location":"api/display/#omgrab.display.screen_writer.ScreenWriter.width","title":"<code>width</code>  <code>property</code>","text":"<p>Display width in pixels.</p>"},{"location":"api/display/#omgrab.display.screen_writer.ScreenWriter.height","title":"<code>height</code>  <code>property</code>","text":"<p>Display height in pixels.</p>"},{"location":"api/display/#omgrab.display.screen_writer.ScreenWriter.display","title":"<code>display(image)</code>","text":"<p>Write an image to the OLED display.</p> <p>The image should be mode '1' (1-bit) with dimensions matching the display (default 128x64). If the hardware is unavailable, this method attempts periodic reconnection.</p> <p>Parameters:</p> Name Type Description Default <code>image</code> <code>Image</code> <p>PIL Image to display.</p> required"},{"location":"api/display/#omgrab.display.screen_writer.ScreenWriter.clear","title":"<code>clear()</code>","text":"<p>Clear the display (set all pixels to black).</p> <p>Safe to call even if the hardware is unavailable.</p>"},{"location":"api/display/#omgrab.display.screen_writer.ScreenWriter.cleanup","title":"<code>cleanup()</code>","text":"<p>Clean up hardware resources.</p> <p>Should be called during shutdown.</p>"},{"location":"api/gpio/","title":"gpio","text":"<p>LED, buzzer, and button control.</p>"},{"location":"api/gpio/#omgrab.gpio.gpio","title":"<code>omgrab.gpio.gpio</code>","text":"<p>GPIO control module for Raspberry Pi 5 LED, buzzer, and button management.</p> <p>This module uses a separate process for GPIO control to avoid GIL contention with video encoding and other CPU-intensive operations in the main process.</p>"},{"location":"api/gpio/#omgrab.gpio.gpio.LEDState","title":"<code>LEDState</code>","text":"<p>               Bases: <code>Enum</code></p> <p>LED state patterns.</p>"},{"location":"api/gpio/#omgrab.gpio.gpio.GPIOController","title":"<code>GPIOController()</code>","text":"<p>Controller for GPIO components: LEDs, buzzer, and push button.</p> <p>This controller delegates all GPIO operations to a separate process, ensuring smooth LED blinking even during heavy CPU load from video encoding or other operations that would normally cause GIL contention.</p> <p>Initialize GPIO controller with a separate process.</p>"},{"location":"api/gpio/#omgrab.gpio.gpio.GPIOController.cleanup","title":"<code>cleanup()</code>","text":"<p>Cleanup GPIO resources.</p>"},{"location":"api/gpio/#omgrab.gpio.gpio.GPIOController.set_led_states","title":"<code>set_led_states(red, green)</code>","text":"<p>Set both LED states.</p> <p>Parameters:</p> Name Type Description Default <code>red</code> <code>LEDState</code> <p>Desired state for red LED.</p> required <code>green</code> <code>LEDState</code> <p>Desired state for green LED.</p> required"},{"location":"api/gpio/#omgrab.gpio.gpio.GPIOController.set_buzzer","title":"<code>set_buzzer(state)</code>","text":"<p>Set buzzer state.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>bool</code> <p>True for ON, False for OFF.</p> required"},{"location":"api/gpio/#omgrab.gpio.gpio.GPIOController.set_buzzer_volume","title":"<code>set_buzzer_volume(volume)</code>","text":"<p>Set buzzer volume via PWM duty cycle.</p> <p>Parameters:</p> Name Type Description Default <code>volume</code> <code>float</code> <p>Volume level from 0.0 (silent) to 1.0 (full).</p> required"},{"location":"api/gpio/#omgrab.gpio.gpio.GPIOController.set_buzzer_tone","title":"<code>set_buzzer_tone(freq)</code>","text":"<p>Set buzzer tone frequency.</p> <p>Parameters:</p> Name Type Description Default <code>freq</code> <code>float</code> <p>Tone frequency in Hz (clamped to 20-20000).</p> required"},{"location":"api/gpio/#omgrab.gpio.gpio.GPIOController.read_button","title":"<code>read_button()</code>","text":"<p>Read the state of the push button.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if button is pressed, False otherwise.</p>"},{"location":"api/gpio/#omgrab.gpio.gpio.GPIOController.buzzer_beep","title":"<code>buzzer_beep(duration=0.1, count=1)</code>","text":"<p>Sound the buzzer for specified duration and count.</p> <p>Parameters:</p> Name Type Description Default <code>duration</code> <code>float</code> <p>Duration of each beep in seconds.</p> <code>0.1</code> <code>count</code> <code>int</code> <p>Number of beeps.</p> <code>1</code>"},{"location":"api/recording/","title":"recording","text":"<p>Stream configuration, chunked MKV writing, and stream merging.</p>"},{"location":"api/recording/#omgrab.recording.stream_configs","title":"<code>omgrab.recording.stream_configs</code>","text":"<p>Stream configuration dataclasses for video and data streams.</p>"},{"location":"api/recording/#omgrab.recording.stream_configs.VideoStreamConfig","title":"<code>VideoStreamConfig(width, height, fps, codec='libx264', bitrate=2000000, input_pixel_format='rgb24', output_pixel_format='yuv420p', stream_options=dict(), metadata=dict())</code>  <code>dataclass</code>","text":"<p>Configuration for a video stream.</p> <p>Attributes:</p> Name Type Description <code>width</code> <code>int</code> <p>Frame width in pixels.</p> <code>height</code> <code>int</code> <p>Frame height in pixels.</p> <code>fps</code> <code>float | Fraction</code> <p>Target frames per second.</p> <code>codec</code> <code>str</code> <p>Video codec name (e.g. 'libx264', 'libx265').</p> <code>bitrate</code> <code>int</code> <p>Target bitrate in bits per second.</p> <code>input_pixel_format</code> <code>str</code> <p>Pixel format of incoming frames (e.g. 'rgb24').</p> <code>output_pixel_format</code> <code>str</code> <p>Pixel format for the encoded stream (e.g. 'yuv420p').</p> <code>stream_options</code> <code>dict[str, str]</code> <p>Additional codec options passed to the encoder.</p> <code>metadata</code> <code>dict[str, Any]</code> <p>Arbitrary metadata stored in the MKV stream header.</p>"},{"location":"api/recording/#omgrab.recording.stream_configs.DataStreamConfig","title":"<code>DataStreamConfig(codec='ass', metadata=dict())</code>  <code>dataclass</code>","text":"<p>Configuration for a non-video data stream stored as an MKV subtitle track.</p> <p>Attributes:</p> Name Type Description <code>codec</code> <code>str</code> <p>Subtitle codec name (e.g. 'ass').</p> <code>metadata</code> <code>dict[str, Any]</code> <p>Arbitrary metadata stored in the MKV stream header.</p>"},{"location":"api/recording/#omgrab.recording.chunked_writer","title":"<code>omgrab.recording.chunked_writer</code>","text":"<p>Chunked video writer with per-stream parallel encoding.</p>"},{"location":"api/recording/#omgrab.recording.chunked_writer.ChunkedWriter","title":"<code>ChunkedWriter(name, output_directory, stream_configs, start_chunk_callback=None, sensor_stream_configs=None, chunk_length_s=60.0, max_encoder_queue_size=200, on_error=None)</code>","text":"<p>Chunked video writer with per-stream parallel encoding.</p> <p>Each stream gets its own encoder thread AND its own single-stream MKV container. Chunks are rotated after chunk_length_s seconds of recording time, measured from the first frame's timestamp.</p> <p>All streams share a common timestamp origin per chunk so PTS values are directly comparable across .part files. When the first frame arrives on any stream, its timestamp becomes the origin for the first chunk. On rotation, the origin advances by exactly chunk_length_s.</p> <p>At finalization (chunk boundary or stop), per-stream .mkv.part files are merged into a single multi-stream MKV via ffmpeg -c copy.</p> <p>Rotation protocol (barrier with generation counter): 1. Any encoder detects a frame whose timestamp exceeds the current    chunk's end boundary and sets rotation_requested. 2. All encoders flush their encoder, close their container, and enter    the rotation barrier. 3. The last encoder to arrive merges the per-stream files, advances    the chunk origin, creates new per-stream containers, and advances    the generation. 4. All encoders resume with new encoder/container objects.</p> <p>Initialize the chunked writer.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name for this writer (used in log messages and thread names).</p> required <code>output_directory</code> <code>Path</code> <p>Directory to write video files.</p> required <code>stream_configs</code> <code>dict[str, VideoStreamConfig]</code> <p>Dictionary mapping stream names to their config.</p> required <code>start_chunk_callback</code> <code>Optional[StartChunkCallback]</code> <p>Optional callback to generate chunk IDs. Signature: (name, started_at, file_extension) -&gt; chunk_id. When None, sequential 5-digit IDs are generated automatically.</p> <code>None</code> <code>sensor_stream_configs</code> <code>Optional[dict[str, DataStreamConfig]]</code> <p>Optional sensor stream configurations.</p> <code>None</code> <code>chunk_length_s</code> <code>float</code> <p>Chunk duration in seconds. The first chunk starts when the first frame arrives; subsequent chunks advance by this amount.</p> <code>60.0</code> <code>max_encoder_queue_size</code> <code>int</code> <p>Max queue size per encoder thread.</p> <code>200</code> <code>on_error</code> <code>Optional[Callable[[str], None]]</code> <p>Optional callback invoked when an encoder thread crashes. Called with the stream name that crashed. Fired on a separate daemon thread to avoid deadlock.</p> <code>None</code>"},{"location":"api/recording/#omgrab.recording.chunked_writer.ChunkedWriter.get_encoder_queue","title":"<code>get_encoder_queue(stream_name)</code>","text":"<p>Get the input queue for a stream's encoder thread.</p>"},{"location":"api/recording/#omgrab.recording.chunked_writer.ChunkedWriter.is_data_stream","title":"<code>is_data_stream(stream_name)</code>","text":"<p>Check if a stream name refers to a data stream.</p>"},{"location":"api/recording/#omgrab.recording.chunked_writer.ChunkedWriter.__enter__","title":"<code>__enter__()</code>","text":"<p>Start the writer and return self for use as a context manager.</p>"},{"location":"api/recording/#omgrab.recording.chunked_writer.ChunkedWriter.__exit__","title":"<code>__exit__(exc_type, exc_value, traceback)</code>","text":"<p>Stop the writer on context manager exit.</p>"},{"location":"api/recording/#omgrab.recording.chunked_writer.ChunkedWriter.start","title":"<code>start()</code>","text":"<p>Start all encoder threads.</p> <p>Chunk creation is deferred until the first frame arrives, so the shared timestamp origin matches the actual frame domain rather than the wall clock.</p>"},{"location":"api/recording/#omgrab.recording.chunked_writer.ChunkedWriter.stop","title":"<code>stop()</code>","text":"<p>Signal all encoder threads to stop, join them, and merge the final chunk.</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer","title":"<code>omgrab.recording.py_av_writer</code>","text":""},{"location":"api/recording/#omgrab.recording.py_av_writer.StreamEncoder","title":"<code>StreamEncoder</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Common interface for all stream encoders (video and data).</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer.StreamEncoder.encode","title":"<code>encode(data, timestamp_s)</code>","text":"<p>Encode data and return the resulting packets.</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer.StreamEncoder.flush","title":"<code>flush()</code>","text":"<p>Flush any buffered data and return remaining packets.</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer.VideoStreamEncoder","title":"<code>VideoStreamEncoder(container, width, height, fps, codec, bitrate, input_pixel_format, output_pixel_format, options, timestamp_origin_s, metadata=None)</code>","text":"<p>Encodes video frames into packets without muxing.</p> <p>All PTS values are calculated relative to a shared timestamp origin, ensuring synchronization across streams within the same clip.</p> <p>Initialize the stream encoder.</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>OutputContainer</code> <p>The PyAV container (used only to add the stream).</p> required <code>width</code> <code>int</code> <p>Width of the video stream.</p> required <code>height</code> <code>int</code> <p>Height of the video stream.</p> required <code>fps</code> <code>float | Fraction</code> <p>Nominal frames per second.</p> required <code>codec</code> <code>str</code> <p>Codec name (e.g. 'libx264', 'ffv1').</p> required <code>bitrate</code> <code>int</code> <p>Target bitrate.</p> required <code>input_pixel_format</code> <code>str</code> <p>Input pixel format (e.g. 'rgb24', 'gray16le').</p> required <code>output_pixel_format</code> <code>str</code> <p>Output pixel format (e.g. 'yuv420p', 'gray16le').</p> required <code>options</code> <code>dict[str, str]</code> <p>Codec options.</p> required <code>timestamp_origin_s</code> <code>float</code> <p>Shared timestamp origin in seconds since epoch. All PTS values are calculated relative to this origin.</p> required <code>metadata</code> <code>Optional[dict[str, Any]]</code> <p>Optional metadata dictionary to add to the stream.</p> <code>None</code>"},{"location":"api/recording/#omgrab.recording.py_av_writer.VideoStreamEncoder.encode","title":"<code>encode(frame, timestamp_s)</code>","text":"<p>Encode a frame and return the resulting packets.</p> <p>Parameters:</p> Name Type Description Default <code>frame</code> <code>Frame</code> <p>Frame data to encode.</p> required <code>timestamp_s</code> <code>float</code> <p>Timestamp in seconds since epoch.</p> required <p>Returns:</p> Type Description <code>list[Packet]</code> <p>List of encoded packets (may be empty due to encoder buffering).</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer.VideoStreamEncoder.flush","title":"<code>flush()</code>","text":"<p>Flush buffered frames from the encoder.</p> <p>Returns:</p> Type Description <code>list[Packet]</code> <p>List of remaining encoded packets.</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer.DataStreamEncoder","title":"<code>DataStreamEncoder(container, timestamp_origin_s, codec='ass', metadata=None)</code>","text":"<p>Constructs data packets for a subtitle stream without muxing.</p> <p>All PTS values are calculated relative to a shared timestamp origin, ensuring synchronization across streams within the same clip.</p> <p>Initialize the data stream encoder.</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>OutputContainer</code> <p>The PyAV container (used only to add the stream).</p> required <code>timestamp_origin_s</code> <code>float</code> <p>Shared timestamp origin in seconds since epoch.</p> required <code>codec</code> <code>str</code> <p>Subtitle codec name.</p> <code>'ass'</code> <code>metadata</code> <code>Optional[dict[str, Any]]</code> <p>Optional metadata dictionary to add to the stream.</p> <code>None</code>"},{"location":"api/recording/#omgrab.recording.py_av_writer.DataStreamEncoder.encode","title":"<code>encode(data, timestamp_s)</code>","text":"<p>Construct a data packet.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Raw bytes to store.</p> required <code>timestamp_s</code> <code>float</code> <p>Timestamp in seconds since epoch.</p> required <p>Returns:</p> Type Description <code>list[Packet]</code> <p>A single-element list containing the packet.</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer.DataStreamEncoder.flush","title":"<code>flush()</code>","text":"<p>No-op: raw packet muxing has no encoder to flush.</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer.merge_stream_files","title":"<code>merge_stream_files(part_paths, output_path)</code>","text":"<p>Merge per-stream MKV part files into a single multi-stream MKV.</p> <p>Uses ffmpeg -c copy for a fast packet-level remux (no re-encoding). The merged file is written atomically via a .tmp rename + fsync.</p> <p>Parameters:</p> Name Type Description Default <code>part_paths</code> <code>list[Path]</code> <p>Paths to single-stream .mkv.part files to merge.</p> required <code>output_path</code> <code>Path</code> <p>Final output path (must end in .mkv).</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If ffmpeg fails.</p>"},{"location":"api/recording/#omgrab.recording.py_av_writer.merge_recording_chunks","title":"<code>merge_recording_chunks(recording_dir, output_path)</code>","text":"<p>Merge sequential MKV chunks from a recording directory into one file.</p> <p>Uses ffmpeg concat demuxer for a lossless stream copy. The merged file is written atomically via a .tmp rename + fsync, then the chunk directory is cleaned up.</p> <p>Parameters:</p> Name Type Description Default <code>recording_dir</code> <code>Path</code> <p>Directory containing numbered .mkv chunk files.</p> required <code>output_path</code> <code>Path</code> <p>Final output path (e.g. /data/output/2026-03-17T00:47:42Z.mkv).</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If ffmpeg fails.</p>"},{"location":"api/runtime/","title":"runtime","text":"<p>Recording lifecycle, monitoring, and device management.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager","title":"<code>omgrab.runtime.recording_manager</code>","text":"<p>Recording manager that owns capture device and recording sessions.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingConfig","title":"<code>RecordingConfig(device_ready_timeout_s=7.5, health_check_interval_s=1.0, session_join_timeout_s=10.0, chunk_length_s=60.0, max_queue_size=400)</code>  <code>dataclass</code>","text":"<p>Configuration for the recording manager.</p> <p>Attributes:</p> Name Type Description <code>device_ready_timeout_s</code> <code>float</code> <p>Max time to wait for the capture device to become ready before starting a recording.</p> <code>health_check_interval_s</code> <code>float</code> <p>Interval between device health checks during recording.</p> <code>session_join_timeout_s</code> <code>float</code> <p>Timeout for joining finished recording session threads during shutdown.</p> <code>chunk_length_s</code> <code>float</code> <p>Duration of each recording chunk in seconds.</p> <code>max_queue_size</code> <code>int</code> <p>Maximum size per encoder queue.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingController","title":"<code>RecordingController</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Minimal interface for controlling recording start/stop.</p> <p>Used by StateMachine to decouple from the full RecordingManager.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingController.start_recording","title":"<code>start_recording()</code>","text":"<p>Start a recording. Returns True on success.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingController.stop_recording","title":"<code>stop_recording()</code>","text":"<p>Stop the active recording.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager","title":"<code>RecordingManager(devices, target_cameras, stream_names, stream_configs, spool_dir, output_dir, config=None, on_device_unhealthy=None, on_recording_complete=None, sensors=None, sensor_stream_names=None, sensor_stream_configs=None)</code>","text":"<p>Manages recording sessions and capture device lifecycles.</p> <p>Owns: - Capture devices (opened/closed per recording) - Active recording session (at most one at a time) - Finished sessions pending cleanup</p> <p>The StateMachine calls start_recording/stop_recording; this class handles all the threading and device management internally.</p> <p>Initialize the recording manager.</p> <p>Parameters:</p> Name Type Description Default <code>devices</code> <code>list[CaptureDevice]</code> <p>Capture devices to use for recording. All must be connected for recording to start, and all are monitored for health during recording.</p> required <code>target_cameras</code> <code>list[Camera]</code> <p>List of cameras to capture from.</p> required <code>stream_names</code> <code>list[str]</code> <p>List of stream names, one per camera.</p> required <code>stream_configs</code> <code>dict[str, VideoStreamConfig]</code> <p>Configuration for each video stream.</p> required <code>spool_dir</code> <code>Path</code> <p>Directory to write temporary video chunks.</p> required <code>output_dir</code> <code>Path</code> <p>Directory to write merged final recordings.</p> required <code>config</code> <code>Optional[RecordingConfig]</code> <p>Recording configuration.</p> <code>None</code> <code>on_device_unhealthy</code> <code>Optional[OnDeviceUnhealthyCallback]</code> <p>Optional callback invoked when any device becomes unhealthy during recording.</p> <code>None</code> <code>on_recording_complete</code> <code>Optional[OnRecordingCompleteCallback]</code> <p>Optional callback invoked when a recording has been merged. Called with the path to the merged MKV file.</p> <code>None</code> <code>sensors</code> <code>list[Sensor] | None</code> <p>Optional list of sensors (e.g., IMU).</p> <code>None</code> <code>sensor_stream_names</code> <code>list[str] | None</code> <p>Optional list of sensor stream names, one per sensor.</p> <code>None</code> <code>sensor_stream_configs</code> <code>dict[str, DataStreamConfig] | None</code> <p>Optional configuration for each sensor stream.</p> <code>None</code>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager.is_recording","title":"<code>is_recording</code>  <code>property</code>","text":"<p>True if there is an active session currently capturing.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager.active_recording_id","title":"<code>active_recording_id</code>  <code>property</code>","text":"<p>Get the ID of the active recording, or None if not recording.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager.recording_started_at","title":"<code>recording_started_at</code>  <code>property</code>","text":"<p>Get the start time of the active recording, or None if not recording.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager.set_on_device_unhealthy","title":"<code>set_on_device_unhealthy(callback)</code>","text":"<p>Set the callback for device unhealthy events.</p> <p>This allows setting the callback after construction, which is needed when there are circular dependencies (e.g., StateMachine needs RecordingManager and vice versa).</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Optional[OnDeviceUnhealthyCallback]</code> <p>Callback to invoke when device becomes unhealthy during recording, or None to clear the callback.</p> required"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager.set_on_recording_error","title":"<code>set_on_recording_error(callback)</code>","text":"<p>Set the callback for recording error events (e.g. encoder crash).</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Optional[OnRecordingErrorCallback]</code> <p>Callback to invoke when a recording error occurs, or None to clear the callback.</p> required"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager.start_recording","title":"<code>start_recording()</code>","text":"<p>Start a new recording session.</p> <p>Opens the capture device, creates a new session with timestamp-based recording name, and starts threads.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if recording started successfully, False otherwise.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager.stop_recording","title":"<code>stop_recording()</code>","text":"<p>Stop the current recording session.</p> <p>Stops capture threads immediately, then merges chunks into a final MKV file. The session is moved to finished_sessions for later cleanup.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_manager.RecordingManager.shutdown","title":"<code>shutdown()</code>","text":"<p>Shutdown the manager: stop active session and join all threads.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_session","title":"<code>omgrab.runtime.recording_session</code>","text":"<p>Recording session that owns capture threads and a parallel writer.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_session.RecordingSession","title":"<code>RecordingSession(recording_id, target_cameras, stream_names, stream_configs, spool_dir, start_chunk_callback=None, sensors=None, sensor_stream_names=None, sensor_stream_configs=None, chunk_length_s=60.0, max_queue_size=400, on_error=None)</code>","text":"<p>A single recording session with isolated threads and parallel writer.</p> <p>Owns: - Per-stream encoder queues (via ChunkedWriter) - Capture threads (one per camera/data source) - Encoder threads + mux thread (via ChunkedWriter)</p> <p>Lifecycle: - start(): Start capture threads and parallel writer - stop(): Signal capture threads to stop; writer continues draining - join(): Wait for all threads to finish</p> <p>Initialize the recording session.</p> <p>Parameters:</p> Name Type Description Default <code>recording_id</code> <code>str</code> <p>Unique ID for this recording (fixed for session lifetime).</p> required <code>target_cameras</code> <code>list[Camera]</code> <p>List of cameras to capture from.</p> required <code>stream_names</code> <code>list[str]</code> <p>List of stream names, one per camera.</p> required <code>stream_configs</code> <code>dict[str, VideoStreamConfig]</code> <p>Configuration for each video stream.</p> required <code>spool_dir</code> <code>Path</code> <p>Directory to write video files.</p> required <code>start_chunk_callback</code> <code>Optional[StartChunkCallback]</code> <p>Optional callback to generate chunk IDs. When None, ChunkedWriter generates sequential 5-digit IDs.</p> <code>None</code> <code>sensors</code> <code>list[Sensor] | None</code> <p>Optional list of sensors (e.g., IMU).</p> <code>None</code> <code>sensor_stream_names</code> <code>list[str] | None</code> <p>Optional list of sensor stream names, one per sensor.</p> <code>None</code> <code>sensor_stream_configs</code> <code>dict[str, DataStreamConfig] | None</code> <p>Optional configuration for each sensor stream.</p> <code>None</code> <code>chunk_length_s</code> <code>float</code> <p>Duration of each recording chunk in seconds.</p> <code>60.0</code> <code>max_queue_size</code> <code>int</code> <p>Maximum size per encoder queue.</p> <code>400</code> <code>on_error</code> <code>Optional[Callable[[], None]]</code> <p>Optional callback invoked when the writer crashes. Called once (even if multiple streams fail). Propagates the error upward so the recording can be stopped.</p> <code>None</code>"},{"location":"api/runtime/#omgrab.runtime.recording_session.RecordingSession.stopped","title":"<code>stopped</code>  <code>property</code>","text":"<p>True if the session has been stopped.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_session.RecordingSession.recording_id","title":"<code>recording_id</code>  <code>property</code>","text":"<p>Get the recording ID for this session.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_session.RecordingSession.recording_length_s","title":"<code>recording_length_s</code>  <code>property</code>","text":"<p>Intended recording duration in seconds (capture start to stop signal).</p>"},{"location":"api/runtime/#omgrab.runtime.recording_session.RecordingSession.is_alive","title":"<code>is_alive</code>  <code>property</code>","text":"<p>True if any thread is still running.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_session.RecordingSession.start","title":"<code>start()</code>","text":"<p>Start capture threads and parallel writer.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If already started.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_session.RecordingSession.stop","title":"<code>stop()</code>","text":"<p>Stop the recording session.</p> <p>Joins capture threads, then stops the parallel writer (which joins encoder threads and merges the final chunk). Blocks until complete.</p>"},{"location":"api/runtime/#omgrab.runtime.recording_session.RecordingSession.join","title":"<code>join(timeout=None)</code>","text":"<p>Wait for all threads to finish (including writer drain).</p> <p>Parameters:</p> Name Type Description Default <code>timeout</code> <code>float | None</code> <p>Maximum time to wait in seconds, or None for no timeout.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if all threads finished, False if timeout expired.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor","title":"<code>omgrab.runtime.network_monitor</code>","text":"<p>Network connectivity monitor with hysteresis and backoff.</p> <p>Simplified for SDK use: checks local network and internet reachability. No heartbeats or API probes.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.Status","title":"<code>Status</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Discrete network health states in increasing order of quality.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.Config","title":"<code>Config(dns_timeout=1.0, tcp_timeout=1.0, internet_check_host='dns.google', internet_check_port=443, down_after_failures=3, up_after_successes=1, poll_ok_s=5.0, poll_min_s=1.0, poll_max_s=20.0, jitter_frac=0.1)</code>  <code>dataclass</code>","text":"<p>Configuration values controlling probes, hysteresis, and backoff.</p> <p>Attributes:</p> Name Type Description <code>dns_timeout</code> <code>float</code> <p>Timeout in seconds for DNS resolution probes.</p> <code>tcp_timeout</code> <code>float</code> <p>Timeout in seconds for TCP connection probes.</p> <code>internet_check_host</code> <code>str</code> <p>Hostname used for internet reachability checks.</p> <code>internet_check_port</code> <code>int</code> <p>TCP port used for internet reachability checks.</p> <code>down_after_failures</code> <code>int</code> <p>Consecutive probe failures before transitioning down.</p> <code>up_after_successes</code> <code>int</code> <p>Consecutive probe successes before transitioning up.</p> <code>poll_ok_s</code> <code>float</code> <p>Polling interval in seconds when the network is healthy.</p> <code>poll_min_s</code> <code>float</code> <p>Minimum polling interval in seconds during backoff.</p> <code>poll_max_s</code> <code>float</code> <p>Maximum polling interval in seconds during backoff.</p> <code>jitter_frac</code> <code>float</code> <p>Fraction of the polling interval added as random jitter.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.Snapshot","title":"<code>Snapshot(status, detail, changed_at)</code>  <code>dataclass</code>","text":"<p>Immutable snapshot of the last stable network state.</p> <p>Attributes:</p> Name Type Description <code>status</code> <code>Status</code> <p>Current network health level.</p> <code>detail</code> <code>str</code> <p>Human-readable description of the last probe result.</p> <code>changed_at</code> <code>float</code> <p>Monotonic timestamp when the status last changed.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.NetworkMonitor","title":"<code>NetworkMonitor(cfg, iface=None)</code>","text":"<p>Stateful network monitor with hysteresis and backoff.</p> <p>Initialize the monitor with configuration and optional interface pinning.</p> <p>Parameters:</p> Name Type Description Default <code>cfg</code> <code>Config</code> <p>Configuration for network monitoring.</p> required <code>iface</code> <code>Optional[str]</code> <p>Optional network interface to monitor.</p> <code>None</code>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.NetworkMonitor.snapshot","title":"<code>snapshot</code>  <code>property</code>","text":"<p>Return the current stable snapshot.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.NetworkMonitor.wake","title":"<code>wake()</code>","text":"<p>Wake the monitor loop early.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.NetworkMonitor.register_on_change_callback","title":"<code>register_on_change_callback(on_network_change)</code>","text":"<p>Register a callback to be called on stable network state changes.</p> <p>Note: Callbacks must be registered before the monitor is started.</p> <p>Parameters:</p> Name Type Description Default <code>on_network_change</code> <code>Callable[[Snapshot], None]</code> <p>Callback function to register.</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If the monitor has already started.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.NetworkMonitor.start","title":"<code>start()</code>","text":"<p>Start the monitor thread.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.NetworkMonitor.shutdown","title":"<code>shutdown()</code>","text":"<p>Stop the monitor thread.</p>"},{"location":"api/runtime/#omgrab.runtime.network_monitor.NetworkMonitor.check_once","title":"<code>check_once()</code>","text":"<p>Run a connectivity check: local network, then internet reachability.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status","title":"<code>omgrab.runtime.device_status</code>","text":"<p>Centralized device status and system information manager.</p> <p>This module provides a single source of truth for all device state, system metrics, and operational status. Can be queried by a display module or external consumers.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.StateProvider","title":"<code>StateProvider</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Minimal interface for querying workflow state.</p> <p>Used by DeviceStatusManager to decouple from the full StateMachine.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.StateProvider.get_current_state","title":"<code>get_current_state()</code>","text":"<p>Get the current workflow state as a string.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.StorageInfo","title":"<code>StorageInfo(total_bytes, used_bytes, available_bytes, used_percent)</code>  <code>dataclass</code>","text":"<p>Storage space information.</p> <p>Attributes:</p> Name Type Description <code>total_bytes</code> <code>int</code> <p>Total storage capacity in bytes.</p> <code>used_bytes</code> <code>int</code> <p>Used storage in bytes.</p> <code>available_bytes</code> <code>int</code> <p>Available storage in bytes.</p> <code>used_percent</code> <code>float</code> <p>Percentage of storage used (0-100).</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.CPUInfo","title":"<code>CPUInfo(temperature_celsius, usage_percent, usage_per_core)</code>  <code>dataclass</code>","text":"<p>CPU metrics.</p> <p>Attributes:</p> Name Type Description <code>temperature_celsius</code> <code>Optional[float]</code> <p>CPU temperature in Celsius, or None if unavailable.</p> <code>usage_percent</code> <code>float</code> <p>Overall CPU usage percentage (0-100).</p> <code>usage_per_core</code> <code>list[float]</code> <p>Per-core usage percentages (0-100).</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.CPUInfo.__str__","title":"<code>__str__()</code>","text":"<p>Return human-readable CPU status.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.MemoryInfo","title":"<code>MemoryInfo(total_bytes, used_bytes, available_bytes, used_percent)</code>  <code>dataclass</code>","text":"<p>Memory usage information.</p> <p>Attributes:</p> Name Type Description <code>total_bytes</code> <code>int</code> <p>Total RAM in bytes.</p> <code>used_bytes</code> <code>int</code> <p>Used RAM in bytes.</p> <code>available_bytes</code> <code>int</code> <p>Available RAM in bytes.</p> <code>used_percent</code> <code>float</code> <p>Percentage of RAM used (0-100).</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.MemoryInfo.__str__","title":"<code>__str__()</code>","text":"<p>Return human-readable memory status.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.NetworkInfo","title":"<code>NetworkInfo(status, wifi_ssid, wifi_signal_strength)</code>  <code>dataclass</code>","text":"<p>Network connectivity information.</p> <p>Attributes:</p> Name Type Description <code>status</code> <code>str</code> <p>Connection state, one of 'offline', 'network_only', or 'online'.</p> <code>wifi_ssid</code> <code>Optional[str]</code> <p>Connected WiFi network name, or None if not on WiFi.</p> <code>wifi_signal_strength</code> <code>Optional[int]</code> <p>Signal strength in dBm (e.g. -62), or None if unavailable.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.RecordingInfo","title":"<code>RecordingInfo(is_recording, recording_id, duration_seconds)</code>  <code>dataclass</code>","text":"<p>Active recording information.</p> <p>Attributes:</p> Name Type Description <code>is_recording</code> <code>bool</code> <p>Whether a recording is currently in progress.</p> <code>recording_id</code> <code>Optional[str]</code> <p>Timestamp-based identifier for the active recording, or None.</p> <code>duration_seconds</code> <code>Optional[float]</code> <p>Elapsed recording time in seconds, or None if not recording.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.BatteryInfo","title":"<code>BatteryInfo(percent, voltage_v, current_a, power_w, is_charging)</code>  <code>dataclass</code>","text":"<p>Battery status information.</p> <p>Attributes:</p> Name Type Description <code>percent</code> <code>float</code> <p>Battery percentage (0-100).</p> <code>voltage_v</code> <code>float</code> <p>Battery voltage in volts.</p> <code>current_a</code> <code>float</code> <p>Current draw in amps (positive when charging).</p> <code>power_w</code> <code>float</code> <p>Power consumption in watts.</p> <code>is_charging</code> <code>bool</code> <p>Whether the battery is currently charging.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.BatteryInfo.__str__","title":"<code>__str__()</code>","text":"<p>Return human-readable battery status.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatus","title":"<code>DeviceStatus(device_id, software_version, uptime_seconds, state_machine_state, storage, cpu, memory, network, recording, device_healthy, device_error, battery)</code>  <code>dataclass</code>","text":"<p>Complete device status snapshot.</p> <p>Aggregates all subsystem statuses into a single point-in-time reading, produced by DeviceStatusManager on each update cycle.</p> <p>Attributes:</p> Name Type Description <code>device_id</code> <code>str</code> <p>Unique identifier for this device.</p> <code>software_version</code> <code>str</code> <p>Currently running software version string.</p> <code>uptime_seconds</code> <code>float</code> <p>Seconds since the status manager started.</p> <code>state_machine_state</code> <code>str</code> <p>Current workflow state (e.g. 'idle', 'recording').</p> <code>storage</code> <code>StorageInfo</code> <p>Disk space metrics for the spool directory.</p> <code>cpu</code> <code>CPUInfo</code> <p>CPU temperature and usage metrics.</p> <code>memory</code> <code>MemoryInfo</code> <p>RAM usage metrics.</p> <code>network</code> <code>NetworkInfo</code> <p>Network connectivity and WiFi details.</p> <code>recording</code> <code>RecordingInfo</code> <p>Active recording state and duration.</p> <code>device_healthy</code> <code>bool</code> <p>Whether the device is operating normally.</p> <code>device_error</code> <code>Optional[str]</code> <p>Human-readable error description, or None if healthy.</p> <code>battery</code> <code>Optional[BatteryInfo]</code> <p>Battery metrics, or None if no battery hardware is present.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.SystemMetricsProvider","title":"<code>SystemMetricsProvider</code>","text":"<p>               Bases: <code>Protocol</code></p> <p>Interface for reading system-level hardware and OS metrics.</p> <p>The default implementation reads from Linux /proc and /sys filesystems. Inject an alternative for testing or non-Linux development hosts.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.SystemMetricsProvider.get_cpu_info","title":"<code>get_cpu_info()</code>","text":"<p>Get CPU temperature and usage information.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.SystemMetricsProvider.get_memory_info","title":"<code>get_memory_info()</code>","text":"<p>Get memory usage information.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.SystemMetricsProvider.get_storage_info","title":"<code>get_storage_info(path)</code>","text":"<p>Get storage space information for the given path.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.SystemMetricsProvider.get_wifi_info","title":"<code>get_wifi_info()</code>","text":"<p>Get WiFi SSID and signal strength in dBm.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatusManager","title":"<code>DeviceStatusManager(device_id, software_version, spool_base_dir, system_metrics, enable_battery_monitor=True, status_file=None)</code>","text":"<p>Manages and aggregates device status information from multiple sources.</p> <p>Initialize the device status manager.</p> <p>Parameters:</p> Name Type Description Default <code>device_id</code> <code>str</code> <p>Unique device identifier.</p> required <code>software_version</code> <code>str</code> <p>Current software version.</p> required <code>spool_base_dir</code> <code>Path</code> <p>Base directory for storage calculations.</p> required <code>system_metrics</code> <code>SystemMetricsProvider</code> <p>Provider for system-level metrics (CPU, memory, storage, WiFi).</p> required <code>enable_battery_monitor</code> <code>bool</code> <p>Whether to enable battery monitoring.</p> <code>True</code> <code>status_file</code> <code>Optional[Path]</code> <p>Path to write status JSON file for external readers.</p> <code>None</code>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatusManager.set_state_machine","title":"<code>set_state_machine(state_machine)</code>","text":"<p>Set the state machine reference.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatusManager.set_recording_manager","title":"<code>set_recording_manager(recording_manager)</code>","text":"<p>Set the recording manager reference.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatusManager.set_network_monitor","title":"<code>set_network_monitor(network_monitor)</code>","text":"<p>Set the network monitor reference.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatusManager.start","title":"<code>start()</code>","text":"<p>Start the background worker that updates status periodically.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatusManager.shutdown","title":"<code>shutdown()</code>","text":"<p>Stop the background worker.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatusManager.get_status","title":"<code>get_status()</code>","text":"<p>Get a complete snapshot of device status.</p> <p>Returns:</p> Type Description <code>DeviceStatus</code> <p>DeviceStatus dataclass with all current information.</p>"},{"location":"api/runtime/#omgrab.runtime.device_status.DeviceStatusManager.get_status_dict","title":"<code>get_status_dict()</code>","text":"<p>Get the cached status dictionary.</p> <p>The status is updated every second by the background worker. This method returns the most recent cached value for fast access.</p> <p>Returns:</p> Type Description <code>dict</code> <p>Dictionary representation of device status.</p>"},{"location":"api/runtime/#omgrab.runtime.battery_monitor","title":"<code>omgrab.runtime.battery_monitor</code>","text":"<p>Battery monitoring module for Waveshare UPS HAT B (INA219).</p> <p>This module provides battery status information including voltage, current, power consumption, and battery percentage for devices with the Waveshare UPS HAT B.</p> <p>Based on Waveshare's INA219 example code, adapted for this project's style.</p>"},{"location":"api/runtime/#omgrab.runtime.battery_monitor.BatteryStatus","title":"<code>BatteryStatus(voltage_v, current_a, power_w, percent)</code>  <code>dataclass</code>","text":"<p>Battery status information from the INA219 sensor.</p> <p>Attributes:</p> Name Type Description <code>voltage_v</code> <code>float</code> <p>Load voltage in volts.</p> <code>current_a</code> <code>float</code> <p>Current draw in amps (positive when charging).</p> <code>power_w</code> <code>float</code> <p>Power consumption in watts.</p> <code>percent</code> <code>float</code> <p>Estimated battery percentage (0-100).</p>"},{"location":"api/runtime/#omgrab.runtime.battery_monitor.BatteryStatus.__str__","title":"<code>__str__()</code>","text":"<p>Return human-readable battery status.</p>"},{"location":"api/runtime/#omgrab.runtime.battery_monitor.BatteryMonitor","title":"<code>BatteryMonitor(i2c_bus=1, i2c_addr=66, min_voltage=6.0, max_voltage=8.4)</code>","text":"<p>Monitor battery status using INA219 chip on Waveshare UPS HAT B.</p> <p>The INA219 measures voltage, current, and power. Battery percentage is calculated based on voltage (6V = 0%, 8.4V = 100% for typical 2S Li-ion).</p> <p>Initialize the battery monitor.</p> <p>Parameters:</p> Name Type Description Default <code>i2c_bus</code> <code>int</code> <p>I2C bus number (default: 1).</p> <code>1</code> <code>i2c_addr</code> <code>int</code> <p>I2C address of INA219 chip (default: 0x42 for UPS HAT B).</p> <code>66</code> <code>min_voltage</code> <code>float</code> <p>Minimum battery voltage for 0% (default: 6.0V).</p> <code>6.0</code> <code>max_voltage</code> <code>float</code> <p>Maximum battery voltage for 100% (default: 8.4V).</p> <code>8.4</code>"},{"location":"api/runtime/#omgrab.runtime.battery_monitor.BatteryMonitor.available","title":"<code>available</code>  <code>property</code>","text":"<p>True if battery monitoring hardware is available.</p>"},{"location":"api/runtime/#omgrab.runtime.battery_monitor.BatteryMonitor.get_status","title":"<code>get_status()</code>","text":"<p>Get current battery status.</p> <p>If the battery hardware is not available, this method will periodically attempt to reconnect (every 30 seconds by default).</p> <p>Returns:</p> Type Description <code>Optional[BatteryStatus]</code> <p>BatteryStatus with current measurements, or None if battery monitoring</p> <code>Optional[BatteryStatus]</code> <p>is not available or read fails.</p>"},{"location":"api/runtime/#omgrab.runtime.gpio_manager","title":"<code>omgrab.runtime.gpio_manager</code>","text":"<p>GPIO management for button monitoring and connection status.</p>"},{"location":"api/runtime/#omgrab.runtime.gpio_manager.ButtonConfig","title":"<code>ButtonConfig(long_press_threshold_s=5.0, double_press_window_s=0.5, check_interval_s=0.02)</code>  <code>dataclass</code>","text":"<p>Configuration for button press detection.</p> <p>Attributes:</p> Name Type Description <code>long_press_threshold_s</code> <code>float</code> <p>Hold duration to trigger WiFi reconfiguration.</p> <code>double_press_window_s</code> <code>float</code> <p>Window after a first tap in which a second tap counts as a double-press. Only used in IDLE state.</p> <code>check_interval_s</code> <code>float</code> <p>How often to poll the button GPIO (seconds).</p>"},{"location":"api/runtime/#omgrab.runtime.gpio_manager.button_monitor_loop","title":"<code>button_monitor_loop(gpio_controller, state_machine_obj, config=None, on_wifi_setup_complete=None, on_button_press=None, on_wifi_hotspot_started=None, wifi_manager=None)</code>","text":"<p>Monitor button presses and delegate to state machine.</p> <p>Short press: Start/stop recording (handled by state machine) Double press (IDLE only): Camera preview (handled by state machine) Long press (5+ seconds): Trigger WiFi reconfiguration portal</p> <p>Parameters:</p> Name Type Description Default <code>gpio_controller</code> <code>GPIOController</code> <p>GPIO controller instance.</p> required <code>state_machine_obj</code> <code>StateMachine</code> <p>State machine for managing workflow.</p> required <code>config</code> <code>Optional[ButtonConfig]</code> <p>Button detection configuration.</p> <code>None</code> <code>on_wifi_setup_complete</code> <code>Optional[Callable[[bool], None]]</code> <p>Optional callback invoked when WiFi setup completes.</p> <code>None</code> <code>on_button_press</code> <code>Optional[Callable[[], None]]</code> <p>Optional callback invoked on any button press (rising edge).</p> <code>None</code> <code>on_wifi_hotspot_started</code> <code>Optional[Callable[[], None]]</code> <p>Optional callback invoked when WiFi hotspot is started.</p> <code>None</code> <code>wifi_manager</code> <code>Optional[WifiManager]</code> <p>Optional WifiManager instance for WiFi setup. If None, long-press and WiFi cancel are no-ops.</p> <code>None</code>"},{"location":"api/runtime/#omgrab.runtime.wifi_connect","title":"<code>omgrab.runtime.wifi_connect</code>","text":"<p>WiFi Connect manager with injectable container management functions.</p> <p>The WifiManager class owns the WiFi setup lifecycle (state transitions, callbacks) but delegates container operations to injected functions. This allows the SDK to be independent of Docker while applications can provide Docker-specific implementations.</p>"},{"location":"api/runtime/#omgrab.runtime.wifi_connect.WifiManager","title":"<code>WifiManager(start_container, stop_container, is_running)</code>","text":"<p>Manages WiFi setup lifecycle with injectable container functions.</p> <p>The manager orchestrates the WiFi provisioning flow: 1. Check if the WiFi setup service is already running. 2. Start it via the injected function. 3. Wait for completion (via the start function's blocking behavior). 4. Invoke a callback when complete.</p> <p>Container-specific logic (Docker API calls, process management, etc.) is provided by the caller via the constructor arguments.</p> <p>Initialize the WiFi manager.</p> <p>Parameters:</p> Name Type Description Default <code>start_container</code> <code>Callable[[bool, Optional[Callable[[bool], None]]], bool]</code> <p>Function to start the WiFi setup service. Signature: (force_mode, callback) -&gt; bool. Should return True if the service was started, False otherwise. The callback (if provided) should be invoked with True/False when the service completes.</p> required <code>stop_container</code> <code>Callable[[], bool]</code> <p>Function to stop the WiFi setup service. Should return True if stopped, False otherwise.</p> required <code>is_running</code> <code>Callable[[], bool]</code> <p>Function to check if the service is currently running. Should return True if running, False otherwise.</p> required"},{"location":"api/runtime/#omgrab.runtime.wifi_connect.WifiManager.start_wifi_connect","title":"<code>start_wifi_connect(force_mode=True, callback=None)</code>","text":"<p>Start the WiFi setup service.</p> <p>Parameters:</p> Name Type Description Default <code>force_mode</code> <code>bool</code> <p>If True, forces the portal to start even if WiFi is connected.</p> <code>True</code> <code>callback</code> <code>Optional[Callable[[bool], None]]</code> <p>Optional callback invoked when WiFi setup completes.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the service was started, False otherwise.</p>"},{"location":"api/runtime/#omgrab.runtime.wifi_connect.WifiManager.stop_wifi_connect","title":"<code>stop_wifi_connect()</code>","text":"<p>Stop the WiFi setup service if running.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the service was stopped, False otherwise.</p>"},{"location":"api/runtime/#omgrab.runtime.wifi_connect.WifiManager.is_wifi_connect_running","title":"<code>is_wifi_connect_running()</code>","text":"<p>Check if the WiFi setup service is currently running.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if running, False otherwise.</p>"},{"location":"api/sensors/","title":"sensors","text":"<p>Sensor abstractions for non-video timestamped data such as IMU readings.</p>"},{"location":"api/sensors/#omgrab.sensors.sensor","title":"<code>omgrab.sensors.sensor</code>","text":"<p>Sensor abstraction for non-video timestamped data (e.g., IMU readings).</p>"},{"location":"api/sensors/#omgrab.sensors.sensor.SensorDataUnavailableError","title":"<code>SensorDataUnavailableError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Exception raised when sensor data is not available within the timeout.</p>"},{"location":"api/sensors/#omgrab.sensors.sensor.Sensor","title":"<code>Sensor</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Source of timestamped data payloads (e.g., serialized sensor readings).</p>"},{"location":"api/sensors/#omgrab.sensors.sensor.Sensor.get_next_item","title":"<code>get_next_item(timeout_s=None)</code>  <code>abstractmethod</code>","text":"<p>Get the next data payload.</p> <p>Parameters:</p> Name Type Description Default <code>timeout_s</code> <code>Optional[float]</code> <p>Maximum time to wait in seconds, or None to block.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[bytes, datetime]</code> <p>Tuple of (data_bytes, timestamp).</p> <p>Raises:</p> Type Description <code>SensorDataUnavailableError</code> <p>If no data is available within the timeout.</p>"},{"location":"api/sensors/#omgrab.sensors.sensor.Sensor.setup","title":"<code>setup()</code>  <code>abstractmethod</code>","text":"<p>Setup the sensor.</p>"},{"location":"api/sensors/#omgrab.sensors.sensor.Sensor.close","title":"<code>close()</code>  <code>abstractmethod</code>","text":"<p>Close the sensor.</p>"},{"location":"api/sensors/#omgrab.sensors.sensor.Sensor.__enter__","title":"<code>__enter__()</code>","text":"<p>Enter the context manager.</p>"},{"location":"api/sensors/#omgrab.sensors.sensor.Sensor.__exit__","title":"<code>__exit__(exc_type, exc_value, traceback)</code>","text":"<p>Exit the context manager.</p>"},{"location":"api/sensors/#omgrab.sensors.queue_reader_sensor","title":"<code>omgrab.sensors.queue_reader_sensor</code>","text":"<p>Queue-based sensor that reads timestamped bytes payloads from a queue.</p>"},{"location":"api/sensors/#omgrab.sensors.queue_reader_sensor.QueueReaderSensor","title":"<code>QueueReaderSensor(data_queue)</code>","text":"<p>               Bases: <code>Sensor</code></p> <p>Sensor that reads timestamped bytes payloads from a queue.</p> <p>Initialize the queue reader sensor.</p> <p>Parameters:</p> Name Type Description Default <code>data_queue</code> <code>Queue[tuple[bytes, datetime]]</code> <p>Queue providing (data_bytes, timestamp) tuples.</p> required"},{"location":"api/sensors/#omgrab.sensors.queue_reader_sensor.QueueReaderSensor.setup","title":"<code>setup()</code>","text":"<p>No-op for queue-based sensor.</p>"},{"location":"api/sensors/#omgrab.sensors.queue_reader_sensor.QueueReaderSensor.close","title":"<code>close()</code>","text":"<p>No-op for queue-based sensor.</p>"},{"location":"api/sensors/#omgrab.sensors.queue_reader_sensor.QueueReaderSensor.get_next_item","title":"<code>get_next_item(timeout_s=None)</code>","text":"<p>Get the next data payload from the queue.</p>"},{"location":"api/sensors/#omgrab.sensors.queue_reader_sensor.QueueReaderSensor.flush_queue","title":"<code>flush_queue()</code>","text":"<p>Flush all pending items from the queue.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of items flushed.</p>"},{"location":"api/testing/","title":"testing","text":"<p>Test doubles for use in downstream projects.</p>"},{"location":"api/testing/#omgrab.testing","title":"<code>omgrab.testing</code>","text":"<p>Shared test doubles and helpers for omgrab unit tests.</p> <p>This module provides reusable fake implementations of hardware-dependent components. Import fakes directly in test files, or use the convenience fixtures defined in conftest.py.</p>"},{"location":"api/testing/#omgrab.testing.FakeGPIOController","title":"<code>FakeGPIOController()</code>","text":"<p>               Bases: <code>GPIOController</code></p> <p>Test double for GPIOController that records all calls.</p> <p>Subclasses GPIOController but overrides init to avoid spawning a subprocess or importing hardware-specific modules.</p>"},{"location":"api/testing/#omgrab.testing.FakeGPIOController.set_led_states","title":"<code>set_led_states(red, green)</code>","text":"<p>Record LED state change.</p>"},{"location":"api/testing/#omgrab.testing.FakeGPIOController.set_buzzer","title":"<code>set_buzzer(state)</code>","text":"<p>Record buzzer on/off.</p>"},{"location":"api/testing/#omgrab.testing.FakeGPIOController.set_buzzer_volume","title":"<code>set_buzzer_volume(volume)</code>","text":"<p>Record buzzer volume change.</p>"},{"location":"api/testing/#omgrab.testing.FakeGPIOController.set_buzzer_tone","title":"<code>set_buzzer_tone(freq)</code>","text":"<p>Record buzzer tone change.</p>"},{"location":"api/testing/#omgrab.testing.FakeGPIOController.read_button","title":"<code>read_button()</code>","text":"<p>Return the preconfigured button state.</p>"},{"location":"api/testing/#omgrab.testing.FakeGPIOController.buzzer_beep","title":"<code>buzzer_beep(duration=0.1, count=1)</code>","text":"<p>Record a beep request.</p>"},{"location":"api/testing/#omgrab.testing.FakeGPIOController.cleanup","title":"<code>cleanup()</code>","text":"<p>Record cleanup.</p>"},{"location":"api/testing/#omgrab.testing.FakeCaptureDevice","title":"<code>FakeCaptureDevice(*, connected=True, ready=True, device_type=None)</code>","text":"<p>Test double for CaptureDevice that simulates device lifecycle.</p>"},{"location":"api/testing/#omgrab.testing.FakeCaptureDevice.label","title":"<code>label</code>  <code>property</code>","text":"<p>Return the device label.</p>"},{"location":"api/testing/#omgrab.testing.FakeCaptureDevice.connected","title":"<code>connected</code>  <code>property</code>","text":"<p>Return whether the device is connected.</p>"},{"location":"api/testing/#omgrab.testing.FakeCaptureDevice.ready","title":"<code>ready</code>  <code>property</code>","text":"<p>Return whether the device is ready.</p>"},{"location":"api/testing/#omgrab.testing.FakeCaptureDevice.device_type","title":"<code>device_type</code>  <code>property</code>","text":"<p>Return the device type identifier.</p>"},{"location":"api/testing/#omgrab.testing.FakeCaptureDevice.wait_until_ready","title":"<code>wait_until_ready(timeout=None)</code>","text":"<p>Wait until the device is ready.</p>"},{"location":"api/testing/#omgrab.testing.FakeCaptureDevice.preview","title":"<code>preview()</code>","text":"<p>Return a preview context manager.</p>"},{"location":"api/testing/#omgrab.testing.make_snapshot","title":"<code>make_snapshot(status=network_monitor.Status.ONLINE, detail='test')</code>","text":"<p>Create a network Snapshot for testing.</p>"},{"location":"api/utils/","title":"utils","text":"<p>Helper functions for merge and recovery.</p>"},{"location":"api/utils/#omgrab.utils.merge","title":"<code>omgrab.utils.merge</code>","text":"<p>Boot-time recovery: merge orphaned recording chunks.</p>"},{"location":"api/utils/#omgrab.utils.merge.merge_orphaned_chunks","title":"<code>merge_orphaned_chunks(spool_dir, output_dir)</code>","text":"<p>Scan spool_dir for recording directories with chunks and merge them.</p> <p>This is the boot-time recovery equivalent of the old <code>files.enqueue_orphaned_capture_files()</code>. It handles:</p> <ol> <li>Recording directories with finalized .mkv chunks but no merged output:    merge them into output_dir.</li> <li>Incomplete .mkv.tmp files from crashed writes: delete them.</li> </ol> <p>Parameters:</p> Name Type Description Default <code>spool_dir</code> <code>Path</code> <p>Directory containing recording subdirectories (each with numbered .mkv chunk files).</p> required <code>output_dir</code> <code>Path</code> <p>Directory to write merged output files.</p> required"},{"location":"api/workflows/","title":"workflows","text":"<p>State machine coordinating the full device lifecycle.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine","title":"<code>omgrab.workflows.state_machine</code>","text":"<p>State machine for omgrab workflow management.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.WorkflowState","title":"<code>WorkflowState</code>","text":"<p>               Bases: <code>Enum</code></p> <p>High-level workflow states.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine","title":"<code>StateMachine(gpio_controller, recording_manager)</code>","text":"<p>Manages omgrab state transitions and GPIO updates.</p> <p>Initialize state machine.</p> <p>Parameters:</p> Name Type Description Default <code>gpio_controller</code> <code>GPIOController</code> <p>GPIO controller instance for LED/buzzer control.</p> required <code>recording_manager</code> <code>RecordingController</code> <p>Recording controller for start/stop recording.</p> required"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.on_network_change","title":"<code>on_network_change(snapshot)</code>","text":"<p>Callback for network state changes.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.is_recording","title":"<code>is_recording()</code>","text":"<p>Check if currently in RECORDING state.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if in RECORDING workflow state.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.get_current_state","title":"<code>get_current_state()</code>","text":"<p>Get the current workflow state.</p> <p>Returns:</p> Type Description <code>str</code> <p>Current state as a string</p> <code>str</code> <p>(boot/idle/recording/camera_preview/wifi_setup/shutdown).</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.set_show_alert_callback","title":"<code>set_show_alert_callback(callback)</code>","text":"<p>Set callback for displaying alert messages on screen.</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable[[str], None]</code> <p>Called with a warning message string when the state machine needs to alert the user (e.g. camera error).</p> required"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.set_preview_callbacks","title":"<code>set_preview_callbacks(is_available, start, stop)</code>","text":"<p>Set callbacks for camera preview lifecycle.</p> <p>Parameters:</p> Name Type Description Default <code>is_available</code> <code>Callable[[], bool]</code> <p>Returns True if the display hardware is connected and preview can be started.</p> required <code>start</code> <code>Callable[[], None]</code> <p>Called to start the preview pipeline and display.</p> required <code>stop</code> <code>Callable[[], None]</code> <p>Called to stop the preview pipeline and display.</p> required"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.handle_button_press","title":"<code>handle_button_press()</code>","text":"<p>Handle single button press event.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.handle_double_press","title":"<code>handle_double_press()</code>","text":"<p>Handle double button press event (camera preview).</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.on_device_unhealthy","title":"<code>on_device_unhealthy()</code>","text":"<p>Handle device becoming unhealthy during recording.</p> <p>Called by RecordingManager's health monitor. This method is responsible for stopping the recording (the health monitor only detects and signals).</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.on_recording_error","title":"<code>on_recording_error()</code>","text":"<p>Handle a recording error (e.g. encoder crash).</p> <p>Called by RecordingManager when an encoder thread crashes. Stops the recording, beeps an error pattern, and shows an alert.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.enter_wifi_setup","title":"<code>enter_wifi_setup()</code>","text":"<p>Enter WiFi setup state (triggered by long press).</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if transitioned to WIFI_SETUP, False if not possible.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.exit_wifi_setup","title":"<code>exit_wifi_setup()</code>","text":"<p>Exit WiFi setup state (called when wifi-connect completes).</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if exited WIFI_SETUP, False if not in WIFI_SETUP state.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.shutdown","title":"<code>shutdown()</code>","text":"<p>Public method to shutdown the state machine.</p>"},{"location":"api/workflows/#omgrab.workflows.state_machine.StateMachine.cleanup","title":"<code>cleanup()</code>","text":"<p>Cleanup resources.</p>"}]}